# 环境变量清单

复制此文件为 `.env`，然后修改为你自己的配置值。

## 数据库连接（必须）
# PostgreSQL 数据库连接 URL
# Supabase 示例: postgresql://postgres:你的密码@db.xxx.supabase.co:5432/postgres
DATABASE_URL=postgresql://user:password@localhost:5432/jidian_campus

## JWT 密钥（必须）
# 用于签署用户认证 token，请使用随机字符串
# 生成命令: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-to-your-secret-key

## Vercel Blob 存储（可选）
# 不配置时，图片/视频上传将使用 base64 降级存储
# 从 Vercel Storage 的 Blob 面板获取
BLOB_READ_WRITE_TOKEN=

## 运行环境
NODE_ENV=development

## 端口（本地开发可选）
# 后端服务端口，默认 3000
PORT=3000

## 前端 API 地址（本地开发可选）
# 前端开发时的 API 代理目标
VITE_API_BASE_URL=/api
